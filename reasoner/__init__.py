"""Layer 2/3 — Entity graph, ownership engine, custody + presence FSMs, risk,
behavior rules. Single-process authority for world state (§6, §7). CPU-bound.

This is the thesis. Develop it with synthetic-trajectory unit tests (tests/),
not video — the FSM and ownership logic are pure functions of track streams.
"""
